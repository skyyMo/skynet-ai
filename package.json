{
  "name": "skynet-ai",
  "version": "1.0.0",
  "description": "SkyNet AI - Meeting transcript to development stories processor",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "nodemon server.js",
    "build": "cd client && npm install && npm run build && cd .. && cp -r client/build ./build",
    "install-all": "npm install && cd client && npm install",
    "heroku-postbuild": "cd client && npm install && npm run build && cd .. && cp -r client/build ./build"
  },
  "engines": {
    "node": ">=18.0.0",
    "npm": ">=8.0.0"
  },
  "dependencies": {
    "express": "^4.18.2",
    "cors": "^2.8.5",
    "dotenv": "^16.3.1",
    "@notionhq/client": "^2.2.13",
    "openai": "^4.20.0",
    "node-cron": "^3.0.3"
  },
  "devDependencies": {
    "nodemon": "^3.0.1"
  },
  "keywords": [
    "ai",
    "meeting-notes",
    "jira",
    "notion",
    "slack",
    "automation"
  ],
  "author": "SkyNet AI Team",
  "license": "MIT"
}